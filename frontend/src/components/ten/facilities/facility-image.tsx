/**
 * The facility photograph.
 *
 * Re-exported for the same reason as the data behind it: the banner, its
 * aspect ratio and the gradient fallback carry nothing resident-specific.
 */
export { FacilityImage } from "@/components/res/facilities/facility-image";
