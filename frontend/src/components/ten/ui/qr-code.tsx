/**
 * The square on a visitor pass.
 *
 * Re-exported: the code is drawn from a hash of the pass id and carries nothing
 * resident-specific, so both portals draw the same square for the same id.
 */
export { ResQrCode as TenQrCode } from "@/components/res/ui/qr-code";
